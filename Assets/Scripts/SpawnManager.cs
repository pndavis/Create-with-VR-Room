using UnityEngine.XR.Interaction.Toolkit;
using UnityEngine;

public class SpawnManager : XRBaseInteractable
{
    private GameObject grabbableObject;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
